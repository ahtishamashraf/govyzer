# CRM web application
NEXT_PUBLIC_APP_NAME=Govyzer
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_DEFAULT_COUNTRY=AE
NEXT_PUBLIC_DEFAULT_CURRENCY=AED
NEXT_PUBLIC_DEFAULT_TIMEZONE=Asia/Dubai

# Server-only: the CRM proxies every API call through its own origin so authentication
# cookies keep working on tenant custom domains without third-party cookies.
API_INTERNAL_URL=http://localhost:4000
NEXT_PUBLIC_SENTRY_DSN=
